FROM python:3.12-slim
WORKDIR /app
RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*
RUN pip install --no-cache-dir requests
COPY mediamtx-exporter.py .
EXPOSE 8080
CMD ["python", "mediamtx-exporter.py"]
